{
  "name": "lexis-home",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:chrome.extension": "INLINE_RUNTIME_CHUNK=false react-scripts build && cp ../app.js ./build/app.js && cp ../background.js ./build/background.js && cp ../panel.js ./build/panel.js && cp ../selection.service.js ./build/selection.service.js && cp ../app.css ./build/app.css",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
